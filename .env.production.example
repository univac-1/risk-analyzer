# ===========================================
# GCP Production Environment Variables
# ===========================================

# Google Cloud Project
GOOGLE_CLOUD_PROJECT=your-project-id
GCP_PROJECT_NUMBER=123456789012

# Region
GCP_REGION=asia-northeast1

# ===========================================
# Database (Cloud SQL)
# ===========================================
DB_CONNECTION_NAME=your-project-id:asia-northeast1:risk-analyzer-db
DB_NAME=video_risk_analyzer
DB_PASSWORD=your-secure-password
DATABASE_URL=postgresql+asyncpg://postgres:${DB_PASSWORD}@/${DB_NAME}?host=/cloudsql/${DB_CONNECTION_NAME}

# ===========================================
# Redis (Memorystore)
# ===========================================
REDIS_HOST=10.0.0.3
REDIS_URL=redis://${REDIS_HOST}:6379/0
CELERY_BROKER_URL=redis://${REDIS_HOST}:6379/0
CELERY_RESULT_BACKEND=redis://${REDIS_HOST}:6379/0

# ===========================================
# Storage (Cloud Storage)
# ===========================================
USE_GCS=true
STORAGE_BUCKET=your-project-id-videos
GCS_BUCKET=your-project-id-videos

# ===========================================
# Application Settings
# ===========================================
MAX_FILE_SIZE_MB=100
ALLOWED_EXTENSIONS=mp4

# ===========================================
# GitHub Actions Secrets (Set in GitHub)
# ===========================================
# GCP_PROJECT_ID - GCP Project ID
# GCP_PROJECT_NUMBER - GCP Project Number
# WIF_PROVIDER - Workload Identity Federation Provider
# WIF_SERVICE_ACCOUNT - Service Account for WIF
# DB_CONNECTION_NAME - Cloud SQL Connection Name
# DB_NAME - Database Name
# DB_PASSWORD - Database Password
# REDIS_HOST - Memorystore Redis IP
# GCS_BUCKET - Cloud Storage Bucket Name
